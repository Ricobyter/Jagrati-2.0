import React from 'react'
import { useNavigate } from 'react-router-dom'

export default function Register() {
    const navigate = useNavigate()
  return (
    <div>
      <button className='text-orange-600'>Login</button>
    </div>
  )
}
